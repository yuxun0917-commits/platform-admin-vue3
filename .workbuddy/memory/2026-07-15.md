# 2026-07-15

## 登录后调用 /user/info 渲染菜单（后端驱动菜单）

实现：登录成功后从后端 `GET /user/info` 拉取菜单树并渲染侧边栏，同时把后端菜单注册为 Vue 路由。

### 后端契约（platform-parent）
- `UserInfoVO`: `{ user: UserVO, roles: RoleVO[], permissions: List<String>, menus: List<MenuTreeVO> }`
- `MenuTreeVO`: `id, parentId, menuName, menuType(1目录/2菜单/3按钮), path(相对父级，如 "user"), component(相对 src/views，如 "system/user/index"), redirect, icon(iconify 名如 "ep:user"), perms, displayOrder, isHidden(0/1), isCache(1/0), isExternal(1/0), status, children`
- `sys_menu` 列序：id, parent_id, menu_name, menu_type, path, component, redirect, icon, perms, display_order, is_hidden, is_cache, is_external, status, ...
- `/user/info` 需要登录（未登录返回 `{code:10003,...}`，前端已配置为登出码）。

### 前端改动
- `src/typings/api.d.ts`: 新增 `Api.Auth.MenuTree / UserProfile / RoleInfo / UserInfoDetail`；`Api.Auth.UserInfo` 增加 nickname/avatar/deptName。
- `src/service/api/auth.ts`: `fetchUserInfo()` → `GET /user/info`（替换原 `fetchGetUserInfo`）。
- `src/store/modules/auth/index.ts`: `getUserInfo()` 改用 `fetchUserInfo`，写入 userInfo(roles=roleCode 数组, buttons=permissions) 并新增 `userMenus` ref 存后端菜单树。
- `src/store/modules/route/backend-menu.ts`(新): `transformMenusToRoutes` / `transformMenusToGlobalMenus`。目录(menuType=1)→`layout.base` 父路由；菜单(2)→`import.meta.glob('@/views/**/*.vue')` 解析 component（兼容 `system/user/index` 与 `system/dict/item` 两种写法），找不到则回退到 `coming-soon` 占位页；按钮(3)/isHidden=1 跳过。route name 由全路径推导（如 `/system/user`→`system_user`），与菜单 key/routeKey 一致。
- `src/store/modules/route/index.ts`: 新增 `initBackendMenu()`，在 `initAuthRoute()` 的 static/dynamic 分支之后调用——注册后端路由并 `menus.value = transformMenusToGlobalMenus(userMenus)` 覆盖侧边栏（保留 home/404 等静态路由）。
- `src/views/_builtin/coming-soon/index.vue`(新): 菜单对应前端页面尚未开发时的占位页。
- `src/locales/langs/{zh-cn,en-us}.ts`: `route` 增加 `coming-soon`；`src/typings/app.d.ts` 的 `page.login.common` 增加 `captchaPlaceholder/refreshCaptcha`（修掉之前验证码功能残留的类型错误）。

### 验证
- `pnpm build` 通过；`vue-tsc` 在我改动的文件无新增类型错误（仅剩预先存在且与本任务无关的环境性错误：`axios`/`@unocss/core` 模块解析、toggle-auth 布尔传参——均被 .d.ts 解析错误长期掩盖，vite 构建运行正常）。
- 注意：后端菜单 component 指向 `src/views/system/user/index.vue` 等，本 scaffold 目前不存在这些页面，因此点击对应菜单会显示“页面建设中”占位页，待后续补齐前端视图即可正常加载。
- `VITE_AUTH_ROUTE_MODE` 仍为 `static`（保留 home/演示路由），但侧边栏菜单已被后端 /user/info 覆盖。

## 脚手架整体通读（下午）

通读确认当前进度与债务：

### 已对齐后端（可用）
- `src/service/api/user.ts`(新): 完整的 `/user/**` 与 `/role/select-list`，分页字段已做 Soybean(current/size) ↔ 后端(page/pageSize) 映射。
- `manage_user` 用户列表页 + `user-operate-drawer` + `user-search` + `reset-password-modal`(新) 已用后端 API。
- 登录/验证码契约、后端驱动菜单均已落地（见上）。

### 仍是 Soybean 演示草稿（需清理/迁移）
- `src/service/api/system-manage.ts` 仍是**过时 mock 端点**（`/systemManage/getUserList`, `/getRoleList`, `/getMenuList/v2`, `/getAllPages`, `/getMenuTree`, `/getAllRoles`）——真实后端无这些路径，`manage_role` / `manage_menu` 页面（role/index, menu/index, role/modules/menu-auth-modal, menu/modules/menu-operate-modal）仍 import 这些函数，连真实后端会 404。
- 演示路由/页面仍残留：`home`, `about`, `function/*`, `multi-menu/*`, `user-center`（静态路由，static 模式下与后端菜单并存，可能路径冲突）。
- `constants/business.ts` 同时有后端整数枚举（`userStatusRecord` 0/1、`userGenderRecord` 0/1/2）和 Soybean 遗留字符串枚举（`enableStatusRecord` '1'/'2'）；role/menu 页用遗留字符串版。

## 修复：点击用户管理无页面（20:51）
- 现象：侧边栏“用户管理”点击后无页面（占位页/空白）。
- 根因：① 页面之前建在 `src/views/manage/`，但后端菜单契约 `component` 返回 `system/...`（见上 MenuTreeVO），`backend-menu.ts` 解析到不存在的 `src/views/system/...` → 回退 coming-soon 占位页；② elegant 生成文件不一致：`routes.ts` 残留旧 `manage_user` 命名空间（`component: 'view.manage_user'`），而 `imports.ts/views` 已是 `system_user` → `transformElegantRoutesToVueRoutes` 取 `views['manage_user']` 为 undefined → 抛 "View component not found" → 该路由被丢弃。
- 修复：用 PowerShell `Move-Item`（Git Bash `mv` 在 Windows 报 Permission denied）将 `src/views/manage/{user,role,menu,user-detail}` 移到 `src/views/system/`。移动后 elegant-router 的 vite 插件 **watcher** 自动重生成 `routes.ts/imports.ts/transform.ts/elegant-router.d.ts` 为 `system` 命名空间，与页面路径对齐。
- 验证：临时启动 dev server(8848)，`/src/views/system/user/index.vue` 编译返回 200、16KB JS、无编译错误；已关闭。
- `locales` 中 `route.manage_*` 旧键现为 dead key（侧边栏已被后端 `menuName` 接管），无害。

## 二次修复：点击用户管理仍无页面（21:02）
- 根因深化：首次修复只对齐了 elegant 路由命名空间与页面目录，但 `backend-menu.ts` 的 `resolveViewComponent` 解析后端 `component` 时只做精确匹配（`@/views/${component}.vue` 与 `/index.vue`）。后端 `sys_menu.component` 实际写法若与页面路径前缀不一致（如后端填 `manage/user/index`、裸 `user/index`、`user`），解析不到文件 → 回退 coming-soon/空白。无法自动登录后端核对（验证码严格校验 80003），故改为健壮解析。
- 修复：`resolveViewComponent` → `buildComponentCandidates`：① 剥去常见顶层前缀(system/manage/module/modules/admin/views/web)再试；② 按末级非 index 目录名模糊匹配（如 `system/user/index`→`user`→命中 `@/views/system/user/index.vue`）。解析失败 `console.error` 打印后端原始 component 与全部可用视图。
- 验证：Node 脚本模拟 `system/user/index`/`system/user`/`manage/user/index`/`user/index`/`user` 均命中 `@/views/system/user/index.vue`；dev server(8848) 实测 backend-menu.ts 与 user 页模块编译 200。
- 提示用户：若仍空白，多半后端 component 为特殊值（如 `Layout`/空），请 F12 Console 看 `[backend-menu] 未找到视图组件: component="..."` 红色日志并把该值发回；路由为初始化注册，改动后需硬刷新/Ctrl+F5。

### 其它
- 请求层 `src/service/request/index.ts` 写死了 `apifoxToken` header（mock 工具残留，真实后端不需要，建议移除）。
- 启动端口本地约定 8848，需 `node node_modules/vite/bin/vite.js --mode test --port 8848`（pnpm 不透传 --port）。

## 删除不相关的 Soybean 演示页（晚上）

按 boss 要求删除与后端平台无关的纯 Soybean showcase 页面。

### 已删除（5 个演示页，含子页）
- `src/views/about`、`src/views/function/*`、`src/views/home`、`src/views/multi-menu/*`、`src/views/user-center`
- 用 `git rm -r` 删除，保留 `_builtin/*` 与 `manage/*`。

### 同步清理（elegant-router 生成文件 + 接线）
- 重写 `src/router/elegant/routes.ts`、`src/router/elegant/imports.ts`：仅保留 `_builtin`(403/404/500/coming-soon/iframe-page/login) 与 `manage`(user/role/menu/user-detail)。
- `src/router/elegant/transform.ts` routeMap、`src/typings/elegant-router.d.ts` RouteMap + FirstLevelRouteKey/LastLevelRouteKey union：移除 about/function*/home/multi-menu*/user-center。
- `src/layouts/modules/global-header/components/user-avatar.vue`：移除头部下拉的“个人中心”项（原 `routerPushByKey('user-center')` 真实跳转，不删会 404）。
- `.env`：`VITE_ROUTE_HOME=home` → `manage_user`（home 已删；static 模式下 routeHome 不会被后端菜单覆盖，需指向真实存在的叶子路由，否则“首页”标签指向死路由）。

### 注意 / 未做
- i18n 里 `route.*` 的 about/function*/home/multi-menu*/user-center 死键**未删**（散落在 manage* 真实键之间，逐行删易错且无害，留待后续整体清理）。
- `manage/role/modules/menu-auth-modal.vue` 的 `home.value='home'` 只是占位字符串、不触发跳转，无影响；该 modal 仍依赖过时 mock `fetchGetAllPages/fetchGetMenuTree`，连真实后端会 404（独立问题，本次未动）。
- 本沙箱 `node_modules` 为空，无法跑 `vue-tsc` 校验；改动在类型层面自洽（仅改 .env 值未改类型，被删键已从所有 union/routeMap 清除），需本地 `pnpm install && pnpm typecheck`/`pnpm dev` 复验。

## 排查"进入不到登录页"（紧接删页后）

### 根因 / 修复
- `src/router/routes/builtin.ts` 第 8 行 root 路由 redirect 的 fallback 写死 `/home`（已被删），是删页引入的回归。已改为 `/login`：`redirect: getRoutePath(import.meta.env.VITE_ROUTE_HOME) || '/login'`。
- 路由层面 login **完整可达**（逐项核对）：`imports.ts` 有 `login` 键指向 `src/views/_builtin/login/index.vue`；`elegant/routes.ts` 有 `/login` 路由且 `constant:true`；守卫 `initRoute` 在未登录且目标非常数路由时 `next({name:'login', query:{redirect}})`。login 不会被静默丢弃。

### 结论：不是路由定义 bug，是运行态
- 删 `routes.ts`/`imports.ts` 属**路由结构变更**，Vite HMR **不热更路由单例实例**，必须**重启 dev server** 才能加载新路由表。
- 若 `localStorage` 残留旧 `token`：打开应用判定已登录 → 直接进主界面，看不到登录页（正常已登录行为，非 bug）。
- 沙箱内 `curl localhost:8848` 返回 000 不可信（沙箱网络隔离看不到宿主 localhost），以本机浏览器为准。

### 给 boss 的操作指引
- 重启/启动 dev server（pnpm 不透传 --port）：`node node_modules/vite/bin/vite.js --mode test --port 8848 --host 0.0.0.0`
- 直接看登录页：访问 `http://localhost:8848/login`；若被自动跳走=有旧 token，DevTools→Application→Local Storage 删 `token` 再刷新。
- 后续清理项（未做，与登录无关）：`customRoutes`(routes/index.ts) 的 `exception*`/`document*` 段、`transform.ts` routeMap 同名 Soybean 演示死键——对应视图已不存在，目前被 transform 的 try/catch 静默丢弃、不影响 login，但路由表脏，择机清理。

## 按"保留集"彻底清理路由 + 恢复 Soybean 仪表盘（晚上）

boss 要求只保留：**登录 / 仪表盘 / 用户 / 角色 / 权限**。

### 关键发现：之前的清理编辑全没落盘
- 上一轮我对 routes.ts/imports.ts/transform.ts/d.ts/user-avatar.vue 的删除编辑，因工具并行写锁全部失败，文件一直停在 git 原始完整状态（含 about/function/home/multi-menu/user-center/exception/document）。
- `src/views/about|function|multi-menu|user-center` 的 `git rm` 也一度没真正删（磁盘文件又回来了），本轮用 `git rm -r --force` 才真正删掉。
- 本次改用**整文件 Write 重写**生成文件（避免逐块 Edit 的并行锁丢失）。

### 本轮动作
- 恢复仪表盘：`git checkout HEAD -- src/views/home`（Soybean 原版 dashboard，含 index + 6 个 modules 组件，图表用 `@/hooks/common/echarts`）。
- 删除 demo 视图：`git rm -r --force src/views/about src/views/function src/views/multi-menu src/views/user-center`。
- 整文件重写（只含 _builtin + home + manage）：
  - `src/router/elegant/routes.ts`：generatedRoutes = 403/404/500/coming-soon/iframe/login + home + manage(user/role/menu/user-detail)
  - `src/router/elegant/imports.ts`：views 同步
  - `src/typings/elegant-router.d.ts`：RouteMap + 各 union 只含保留键（删 exception/document/about/function/multi-menu/user-center）
  - `src/router/routes/index.ts`：`customRoutes` 清空为 `[]`（删 exception/document 演示菜单）
  - `src/router/elegant/transform.ts`：routeMap 删 exception/document（与 d.ts RouteMap 对齐，否则类型报 Excess Property）
- `src/layouts/.../user-avatar.vue`：删头部"个人中心"下拉项（原 `routerPushByKey('user-center')` 真实跳转）。
- `.env`：`VITE_ROUTE_HOME=home`（原始版本就如此，仪表盘作为首页）。

### 验证
- Grep 确认 src 内对已删键（user-center/multi-menu/exception/document/about/function）**无代码死引用**（仅 locales 死键 + components.d.ts 的 ExceptionBase 类型，无害）。
- i18n `route.home`('首页') / `route.manage_menu`('菜单管理') / `route.manage_user`('用户管理') 键均在。
- 最终页面集：登录(_builtin/login) / 仪表盘(home) / 用户(manage_user) / 角色(manage_role) / 权限(manage_menu=菜单管理) + 必需框架页(403/404/500/coming-soon/iframe)。

### 注意
- 删/改路由属结构变更，需**重启 dev server**（`node node_modules/vite/bin/vite.js --mode test --port 8848 --host 0.0.0.0`，pnpm 不透传 --port）。
- `manage` 路由带 roles 控制（user/menu=R_ADMIN，role=R_SUPER），需对应角色才显示菜单。
- i18n 里 about/function/multi-menu/user-center/exception/document 的 route.* 死键**未清**（不被引用，无害，留后续）。
- 沙箱 `node_modules` 空，未能跑 `vue-tsc`；改动类型层自洽（d.ts 与 transform routeMap 同键、generatedRoutes 的 name ⊂ RouteKey），建议本地 `pnpm install && pnpm typecheck` 复验。

## 密码策略：必须含 ! 号（20:08）
- 需求：密码需包含 `!` 号。
- `src/constants/reg.ts`：`REG_PWD` 由 `/^\w{6,18}$/` 改为 `/^(?=.*!)[\w!]{6,18}$/`（6-18位，含字母数字下划线且至少一个 `!`）。注 `!` 不在 `\w`，故字符集扩为 `[\w!]` 并加正向预查。
- `src/views/_builtin/login/modules/pwd-login.vue`：默认示例密码 `'123456'` → `'123456!'`。
- `src/locales/langs/zh-cn.ts` / `en-us.ts`：`form.pwd.invalid` 文案同步提示"必须含 ! 号"。
- 该策略全局生效（login/register/reset-pwd 共用 `formRules.pwd`）。如后端 demo 账号密码不含 `!`，前端校验会拦截，登录需输入带 `!` 的密码。

## 登录链路接通：token → Bearer → /user/info → 后端菜单 → 仪表盘（20:12）
- 修 bug：`auth/index.ts` `loginByToken` 第116行 `token.value = loginToken.token` → `token.value = loginToken`（LoginToken 已是字符串，原写法致 `isLogin` 状态丢失）。
- `service/api/auth.ts`：`fetchGetUserInfo` URL 由 `/auth/getUserInfo` 改为 `/user/info`（对齐后端契约）。
- `typings/api.d.ts`：`Auth` namespace 补 `MenuTree` 接口（menuType/path/component/icon/redirect/isHidden/isCache/isExternal/displayOrder/menuName/children，按 MenuTreeVO 约定）；`UserInfo` 加必填 `menus: MenuTree[]`。`auth/index.ts` 的 `userInfo` 初始化补 `menus: []`（避免 TS 必填缺失）。
- `store/modules/route/index.ts`：import `transformMenusToRoutes/GlobalMenus`（backend-menu.ts）；新增 `initBackendMenu()`（从 `authStore.userInfo.menus` 注册后端路由 + 覆盖 `menus.value` 侧边栏），在 `initAuthRoute` 末尾（static/dynamic 分支之后）调用并导出。后端菜单链路此前从未真正接通（initBackendMenu 零调用、Api.Auth.MenuTree 类型缺失）。
- `service/request/index.ts`：移除写死的 mock header `apifoxToken`（真实后端不需要）。
- Bearer 注入确认：`request/index.ts` 拦截器用 `getAuthorization()`（shared.ts 返回 `Bearer ${token}`）写入 headers。
- 完整登录后流程：login → fetchLogin 返回单 token 字符串 → localStg 存 token → fetchGetUserInfo(/user/info) 填 userInfo(含 menus) → redirectFromLogin 跳 routeHome(home=仪表盘) → 守卫 initAuthRoute → initBackendMenu 用 menus 渲染侧边栏。
- 注意：前端后端菜单渲染依赖后端 /user/info 真实返回 MenuTreeVO 结构。若后端返回的是 id/label/pId 树（api.d.ts:222 那个 MenuTree 属别的 namespace，非 Auth），需调整 backend-menu.ts 或类型。沙箱 node_modules 空未跑 vue-tsc，建议本地 `pnpm typecheck` 复验。

## 用户管理模块（/user/**）落地（20:38）
- 此前 manage_user 页面是坏掉的 Soybean 演示页：`index.vue` 调已删的 `fetchGetUserList`，`user-operate-drawer.vue` 调 mock `fetchGetAllRoles`，删除/编辑是空 stub；且 `user.ts` 引用的 `BackendPagingResult/UserSaveVO/UserEditVO/UserPasswordVO/UserSelectVO/EnumVO` 类型在 api.d.ts 全未定义，`api/index.ts` 也没导出 `./user`。
- 类型补齐（`typings/api.d.ts` SystemManage）：`BackendPagingResult<T>{records,page,pageSize,total}`、`EnumVO{code,desc}`、`UserSaveVO`(Pick+password)、`UserEditVO`(Pick含id)、`UserPasswordVO{userId,password}`、`UserSelectVO`。`UserSearchParams` 改为 `{keyword?,status?,userGender?,current,size}` 对齐后端 `/user/page`(keyword+status) 契约。
- `service/api/index.ts` 增加 `export * from './user'`，使 `fetchUserPage` 等可被 `@/service/api` 引用。
- `views/manage/user/index.vue` 重写：列表走 `fetchUserPage`；删除(`fetchUserDelete`)/批量删除/状态切换(`fetchUserEditStatus`)真实接线；新增重置密码弹窗(`user-reset-pwd-modal.vue`) 走 `fetchUserChangePassword`；行内按钮按 `/user/info` 的 `buttons` 做权限门控（`can(code)`：buttons 为空=放开，否则需含 code）。权限码约定：`system:user:add/edit/delete/resetPwd/status`（需与后端 buttons 一致）。
- `modules/user-operate-drawer.vue` 重写：角色下拉改 `fetchRoleSelectList`(GET /role/select-list, 映射 roleCode→value)；提交 add→`fetchUserAdd`、edit→`fetchUserEdit`；新增密码字段(仅 add) 走 `formRules.pwd`(含!号校验)。
- `modules/user-search.vue` 重写：keyword + 性别 + 状态，对齐后端 page 契约。
- i18n（`app.d.ts` schema + zh-cn + en-us 三处同步）：`page.manage.user` 加 `password`/`resetPwd`，`page.manage.user.form` 加 `password`；启用/禁用按钮复用已有的 `manage.common.status.enable/disable`。
- 数据流确认：`request` 的 `transformBackendResponse` 返回 `response.data.data`（解包 payload），故 `fetchUserPage` 的 `.then` 里 `res.data` 即 `BackendPagingResult`，`useTable` transformer 取 records/current/size/total 正确。
- 待验证：沙箱 node_modules 空未跑 vue-tsc/启动；建议本地 `pnpm install && pnpm dev` 复验。后端 `/user/**` 实际字段/权限码需与脚手架一致（见 platform-scaffold-dev skill 的 User 模块契约）。

## 修复：每次点击侧边菜单都重复调用 /user/info（21:13）
- 现象：点击任一左侧菜单，Network 里就出现一次 `/user/info` 请求。
- 根因：`guard/route.ts:141` 在每次路由守卫（`beforeEach→initRoute`）里**无条件**执行 `routeStore.onRouteSwitchWhenLoggedIn()`；该函数体被写成 `await authStore.initUserInfo()`；而 `initUserInfo()→getUserInfo()` 每次都 `await fetchGetUserInfo()`（拉 /user/info，无缓存）。因此每点一次菜单导航就发一次。
- 实际 `/user/info` 在登录后第一次 `initAuthRoute`（`route/index.ts:182` 的 `if(!userInfo.userId) await initUserInfo()`）已拉取过一次，后续无需重复。
- 修复：`route/index.ts` 的 `onRouteSwitchWhenLoggedIn()` 改为空实现（仅留注释说明菜单数据初始化时拉取一次、需刷新请显式调 `authStore.initUserInfo()`）。守卫调用保留，无副作用。
- 验证：dev server(8848，用户常驻) 上 `route/index.ts` 编译 200、无错误。改动已通过 HMR 生效。

## 修复：用户管理页面进了但没表格（21:22）
- 现象：点"用户管理"能进页面（不再是"无页面"占位），但表格无数据。
- 排查：Bearer 链路正确（存/取同为 `token`，`/user/info` 成功证明 token 有效）；`useTable` 的 `immediate` 默认 true（packages/hooks/src/use-table.ts:67），挂载即拉；`GET /user/page` 接口存在（curl 返回 10003 非 404）。→ 锁定为**后端返回分页字段名与前端映射不匹配**导致 `records` 解析为空。
- 原 `fetchUserPage` 只认 `{records, page, pageSize, total}`。若后端用若依 `IPage{records,total,size,current}` 或 `{list/rows/content,...}` 则全空。
- 修复：`src/service/api/user.ts` 的 `fetchUserPage` 改为**容错映射**：records 兼容 `records/list/rows/content`；current 兼容 `current/page/pageNum/pageNo`；size 兼容 `size/pageSize/limit`；total 兼容 `total/totalCount/totalElements`；并在 `res.error` 时 `console.error('[user/page] 请求失败')`、records 为空时 `console.warn('[user/page] 记录为空，原始 data 结构:', d)`。
- 验证：dev server(8848) 上 `user.ts` 编译 200、无错误。改动经 HMR 生效。
- 待 boss 复验：硬刷新后看表格是否出数据；若仍空，F12 Console 看 `[user/page]` 日志把原始 data 结构发回，可一次性对齐。后端源码不在本机（Desktop 下无 UserController.java），无法本地核对契约。

## 修复：字段名错位导致表格单元格全空（"没有表格"根因，21:35）
- 现象：boss 复验仍报"没有表格"。
- **根因（已确认）**：前端 `User` 列表列 `dataIndex` 与后端 `UserVO` 字段名完全错位。前端旧用 `userName/userGender/nickName/userPhone/userEmail`，后端 `UserVO` 真实字段是 `username/nickname/gender/phone/email`（gender 0未知/1男/2女；status 1正常/2禁用）。→ 行能渲染但所有单元格为空，用户感知为"没有表格"。
- 后端契约（同级 `platform-parent` 已读源码确认）：
  - `SysUserController.page(Integer page, Integer pageSize, Integer status, String keyword)` → `GET /user/page`（无 gender 入参，前端传 gender 被忽略，属非阻断项）。
  - `Paging<T>`：`@JsonIgnoreProperties({"size","current","pages"})`，响应 `data` = `{records,total,page,pageSize}`。
  - `UserVO`：id/username/nickname/avatar/gender/birthday/email/phone/.../status/statusText/...。
- 修复（统一对齐后端真实字段名）：
  - `src/typings/api.d.ts`：`User`→`{username,nickname,gender,phone,email,userRoles?,status}`；`UserSearchParams`=Pick(User,'status'|'gender')；`UserSaveVO`/`UserEditVO`/`UserSelectVO` 同步用后端名。
  - `src/views/system/user/index.vue`：列 `dataIndex` 改 `username/gender/nickname/phone/email`（**column key 故意保留 `userName/userGender/nickName/userPhone/userEmail` 以兼容列勾选缓存**）；性别 customRender 在 `gender==='0'` 时返回 `-`；`openResetPwd` 中 `record.userName`→`record.username`；`apiParams` 中 `userGender:undefined`→`gender:undefined`。
  - `src/views/system/user/modules/user-search.vue`：性别筛选 `name="userGender"`/`v-model:model.userGender`→`name="gender"`/`v-model:model.gender`（i18n label key 仍用 `page.manage.user.userGender` 等，不变）。
  - `src/views/system/user/modules/user-operate-drawer.vue`：Model/rules/handleSubmit/模板 全部从 `userName/userGender/nickName/userPhone/userEmail` 改为 `username/gender/nickname/phone/email`。
  - `src/service/api/user.ts` 的 `fetchUserPage` 已把 Soybean `current/size` 转为后端 `page/pageSize`（请求侧已对齐），响应侧容错映射沿用上一轮。
- 残留的 `userGender/nickName/userPhone/userEmail` 引用全部是：常量 map（`userGenderRecord`/`userGenderOptions`）、column key（缓存兼容）、i18n label key（`$t(...)`）、`reset-password-modal.vue` 的独立 prop `userName`——均与列表数据字段无关，无需改。
- 验证：8848 dev server 上 `index.vue` 编译 200，且脚本虚拟模块已确认含 `gender:undefined`/`dataIndex:'username'`/`pwdUserName.value = record.username`，改动经 HMR 生效。
- 待 boss 复验：浏览器硬刷新（Ctrl+F5）用户管理页，表格应出数据；性别列 0 显示 `-`。

## 实测确认：/user/page 真实响应与对齐契约一致（21:52）
- boss 贴出 `GET /user/page` 真实返回：`code:200` + `data:{records:[{id,username,nickname,avatar,gender,birthday,email,phone,deptId,deptName,remark,status,statusText,loginIp,loginDate,createTime}×3], total:3, page:1, pageSize:10}`。
- 字段名（`username/nickname/gender/phone/email/status`）与上一轮对齐的 `User`/`dataIndex` 完全一致；分页结构（`records/total/page/pageSize`）被 `fetchUserPage` 容错映射命中。→ 表格渲染与性别/状态 Tag 渲染均正确，无需再改。
- 实测样例：lisi(gender=2 女/status=1)、zhangsan(gender=1 男/status=1)、admin(gender=0→显示`-`/status=1)。
